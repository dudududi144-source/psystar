{
  "name": "psystar",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "PSYSTAR - canonical psychedelic PSY device: rhythm core, synthesis engine, DMT-grade visual console",
  "scripts": {
    "dev": "bun run src/main.ts",
    "test": "bun test",
    "check": "bun run scripts/check.ts",
    "manifest": "bun run scripts/generate-manifest.ts",
    "build": "bun build src/main.ts --outdir dist --target bun"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.4.0"
  }
}
